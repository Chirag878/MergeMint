{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm --version && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}